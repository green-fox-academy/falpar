
'use strict'

// -  Log each the element of `numList` to the console*
  // *hint: use a loop, console.log(numList) won't cut it		  // *hint: use a loop, console.log(numList) won't cut it
  // -  bonus for using the correct built in array method		  // -  bonus for using the correct built in array method
let numList = [4, 5, 6, 7];
let i = numList.map(function(elem) {
    return elem;
});
console.log(i);